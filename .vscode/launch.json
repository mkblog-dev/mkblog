{
 "version": "0.2.0",
 "configurations": [
     {
         "name": "Debug CLI with Args",
         "type": "go",
         "request": "launch",
         "mode": "debug",
         "program": "${workspaceFolder}/cli",
         "args": [
             "-d", "playground",
             "-o", "dist"
         ],
         "cwd": "${workspaceFolder}"
     }
 ]
}